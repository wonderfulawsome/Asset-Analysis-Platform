FastAPI 기반 REST API 레이어. app.py에서 CORS·정적파일·Jinja2·APScheduler를 설정하고 7개 라우터를 등록. regime(HMM 국면), macro(거시지표), index_feed(ETF 가격), sector_cycle(경기국면), crash_surge(폭락/급등), market_summary(AI 해설), chart(차트+예측), tracking(방문 추적) 엔드포인트 제공.
