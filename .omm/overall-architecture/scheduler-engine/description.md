APScheduler BackgroundScheduler that orchestrates the data pipeline. Runs two interval jobs: a light pipeline every 10 minutes (recent data refresh + real-time prediction using existing models) and a full pipeline every 3 hours (historical data collection + model retraining). Controlled by RUN_SCHEDULER environment variable. Defined in scheduler/job.py with run_pipeline() as the entry point.
