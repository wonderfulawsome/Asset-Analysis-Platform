ML 모델 파일 저장소(models/ 디렉토리). noise_hmm.pkl(HMM Noise 국면 모델), crash_surge_xgb.pkl(XGBoost 폭락/급등 모델), fred_cache.pkl(FRED 데이터 캐시) 등 직렬화된 모델 번들 저장. Dockerfile에서 mkdir -p models로 디렉토리 보장. 스케줄러 초기화 시 모델 존재 여부로 파이프라인 트리거 판단.
