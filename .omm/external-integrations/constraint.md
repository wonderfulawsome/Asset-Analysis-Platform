FRED API는 FRED_API_KEY 환경변수 필요(없으면 CSV fallback). Yahoo Finance v8 API는 curl_cffi로 rate limiting 우회. Supabase는 SUPABASE_URL과 SUPABASE_KEY 필수. CBOE 스크래핑은 HTML 구조 변경에 취약. Shiller Excel URL은 변경될 수 있음. Railway는 Dockerfile + 실패 시 재시작 정책. Groq API는 GROQ_API_KEY 필수.
